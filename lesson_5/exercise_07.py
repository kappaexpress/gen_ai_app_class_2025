#!/usr/bin/env python3
# 練習問題 7: 関数

"""
問題7: 関数を作成して使ってください。

1. 引数なし、戻り値なしの関数 say_hello() を作成し、
   "こんにちは" と表示する関数を作って呼び出す

2. 引数として名前を受け取り、「こんにちは、〇〇さん」と表示する
   関数 greet(name) を作成して呼び出す

3. 引数として2つの数値を受け取り、その和を返す
   関数 add(a, b) を作成し、結果を表示する

4. 引数としてリストを受け取り、その中の最大値を返す
   関数 find_max(numbers) を作成し、[3, 15, 8, 22, 7] に対して使用する

5. 引数として整数 n を受け取り、1から n までの合計を返す
   関数 sum_up_to(n) を作成し、10を渡して結果を表示

期待される出力例:
こんにちは
こんにちは、太郎さん
8
22
55
"""

def main():
    # 問題7-1: say_hello() を作成して呼び出す
    # ここにコードを書いてください

    # 問題7-2: greet(name) を作成して呼び出す
    # ここにコードを書いてください

    # 問題7-3: add(a, b) を作成して呼び出す
    # ここにコードを書いてください

    # 問題7-4: find_max(numbers) を作成して呼び出す
    # ここにコードを書いてください

    # 問題7-5: sum_up_to(n) を作成して呼び出す
    # ここにコードを書いてください


# ここに関数の定義を書いてください


if __name__ == "__main__":
    main()
